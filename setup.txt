

// install rust
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh


// install go
git clone https://github.com/rutu-sh/cloudlab-tools.git && \
  cd cloudlab-tools/tools/generic && \
  make install-go && \
  cd && \
  source ${HOME}/.profile


// setup procman 

git clone https://github.com/proc-manager/procman.git && \
    cd procman && \
    git checkout dev && \
    cd cmd && \
    go build -o procman && \
    sudo cp procman /bin/procman && \
    cp -r alpine-basic ~ && \
    cd 


// sample run 

sudo procman v0.0.1


// setup procmand 
git clone https://github.com/proc-manager/procmand.git && \
    cd procmand

